[package]
name = "device-cuda"
version = "0.1.0"
edition = "2021"
build="build/main.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
device_api = {package="device-api", version="*", path="../device-api"}
num_enum = "0.5.1"

[build-dependencies]
bindgen = "0.61.0"                # Automatically generates Rust FFI bindings to C and C++ libraries.